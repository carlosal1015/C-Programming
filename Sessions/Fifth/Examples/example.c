#include <stdio.h>
main()
{
    /*char a[20];

    printf("Digite su nombre:\n");
    gets(a);

    printf("%s", a);
    */
   /*int array[5] = {5,7,2,0,1};
   int i, j, aux;

   for(i=0;i<5;i++){
       for(j=0;j<5;j++){
           if(array[j]>array[j+1]){
               aux = array[j];
               array[j] = array[j+1];
               array[j+1] = aux;
           }
       }
   }

    for(i=0;i<5;i++){
        printf("%i", array[i]);
    }
    */
   struct Team
   {
        int score1[32];
        int score2[32];
        int score3[32];
   };
   Team TEAMS[32];
}